/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ToTexPipe } from './to-tex.pipe';

describe('ToTexPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTexPipe();
    expect(pipe).toBeTruthy();
  });
});
